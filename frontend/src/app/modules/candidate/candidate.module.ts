import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * CandidateModule - components have been moved to AppModule declarations
 * and are routed directly from AppRoutingModule under the /candidate portal.
 * This module is kept as a placeholder to avoid breaking any existing imports.
 */
@NgModule({
  imports: [CommonModule]
})
export class CandidateModule { }
