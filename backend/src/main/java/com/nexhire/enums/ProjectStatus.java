package com.nexhire.enums;

/** P-Claude.md "PROJECTS": "When Vacancies become zero, Project Status should automatically
 *  become FILLED instead of Inactive." INACTIVE remains a separate, manual Admin action. */
public enum ProjectStatus {
    ACTIVE,
    FILLED,
    INACTIVE
}
